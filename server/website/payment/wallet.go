package payment

/* TODO: Recode own address generation */

func generateAddress(currency, id string) string {
	//strings.Split(id, "-") for new Invoice IDs

	//log.Println("New "+currency+" address:", address, " with ID:", intID)

	return "btc123"
}
